# 粘贴 markdown 解析测试设计（P 系列）

> 场景矩阵：P1-PG，约 40 个场景，分 A-G 七批。
> 关联文档：`testing-plan-edit-switch.md`（S1-S26 编辑态切换 + H1-H5 卡片 hover）。
> 状态：📋 待自动化（test_paste_markdown.py，预计 2026-08-14 开发）——TDD：先写测试（当前红色），再实现 B。

## 背景

**现状**：编辑器粘贴 markdown 是纯文本（tiptap 无 markdown paste 解析）——粘贴 `# 标题` 显示为原文，
保存退出后阅读态才渲染成 H1——**编辑态与阅读态显示不一致（体验割裂）**。

**目标（方案 B）**：粘贴 markdown 全量解析——`handlePaste` 对纯文本剪贴板 → `renderMarkdown`（复用现有
marked 渲染）→ HTML 插入编辑器 → **编辑态即所见即所得**，与阅读态一致。

**方案取舍**：
- 不选 C（tiptap-markdown 扩展）：引入新依赖 + 与现有 Turndown 输出链路冲突
- 不选 A（只解析链接）：粘贴的标题/列表等不渲染，与"# 空格→H1"的即时渲染体验割裂——不满足"粘贴后看见渲染结果"
- **选 B**：复用阅读态同一渲染内核（marked），粘贴结果 = 阅读态结果，一致性最好；保存立刻进阅读态时
  往返对称性被即时检验（编辑态 vs 阅读态不一致会立即暴露）

**关键风险点**：`renderMarkdown` 输出的 ref-link（`href="javascript:void(0)" + data-ref-path`）进 tiptap
时 link mark 的 href 会取到 `javascript:void(0)`——需验证/适配（doc.js 已有编辑态 href 恢复逻辑 174-184、
Turndown ref 输出 837-844，粘贴插入的 ref-link 带上 `data-ref-path` 应能走通保存链路，需实测往返）。

## 测试前提（fixture）

- 复用 `hover_docs` 模式：无空格路径的根公共知识文档（空格路径 ref 是独立待办，见 testing-plan-edit-switch.md 第 7 节）
- 粘贴方式：Playwright 在编辑器选区模拟 paste（`page.evaluate` 派发 paste 事件 + clipboardData 纯文本）

---

## 批 P-A：行内元素

| # | 场景 | 期望（B 实现后）| 当前 |
|---|---|---|---|
| PA1 | 粘贴 `**加粗**` | 编辑态渲染为 strong | ❌ 纯文本 |
| PA2 | 粘贴 `*斜体*` / `<u>下划线</u>`（项目保留语法）| 渲染 italic/underline | ❌ |
| PA3 | 粘贴 `~~删除线~~` | 渲染 strike | ❌ |
| PA4 | 粘贴 `` `行内代码` `` | 渲染 code | ❌ |
| PA5 | 粘贴 `[text](https://...)` | 外链（ext）| ❌ |
| PA6 | 粘贴 `![alt](url)` | 图片 | ❌ |

## 批 P-B：块级元素

| # | 场景 | 期望 | 当前 |
|---|---|---|---|
| PB1 | 粘贴 `# 标题` / `## 二级` / `### 三级` | H1-H3 | ❌ |
| PB2 | 粘贴 `- 无序项`（含 `*`）| 无序列表 | ❌ |
| PB3 | 粘贴 `1. 有序项` | 有序列表 | ❌ |
| PB4 | 粘贴 `- [ ] 待办` | 任务列表 | ❌ |
| PB5 | 粘贴 `> 引用` | blockquote | ❌ |
| PB6 | 粘贴 ``` ```代码块``` ``` | 代码块 | ❌ |
| PB7 | 粘贴 `---` | 分割线 | ❌ |
| PB8 | 粘贴 `\| a \| b \|` 表格 | 表格 | ❌ |

## 批 P-C：组合与嵌套（易错区）

| # | 场景 | 关注点 |
|---|---|---|
| PC1 | 多元素混排（标题+段落+列表+代码块）| 整段粘贴按结构还原 |
| PC2 | 嵌套列表（多级缩进）| 层级保留 |
| PC3 | 列表项内嵌行内格式（`- **加粗** 项`）| 行内元素嵌块级 |
| PC4 | 表格单元格内格式（代码/链接）| 单元格内渲染 |
| PC5 | **代码块内 markdown 语法不解析**（```` ``` `# 这不是标题` ``` ````）| 代码块内容原样 |
| PC6 | 长文本 md（整篇粘贴）| 飞书长 md 不准的场景，我们的稳定性 |
| PC7 | 中文混排 + 特殊字符（emoji/HTML 实体）| 编码安全 |

## 批 P-D：ref 链接专项（B 核心风险）

| # | 场景 | 关注点 |
|---|---|---|
| PD1 | 粘贴 `[text](ref:path)` | 编辑态 ref-link + 保存后阅读态可点 |
| PD2 | 粘贴 `[text](ref:path::章节)` | 章节锚点保留 |
| PD3 | 粘贴 `ref: `（空/空格）→ **不创建空链接**（纯文本）| 空目标拦截 |
| PD4 | 粘贴 ref 路径含空格（%20 编码）| 路径编码正确 |
| PD5 | 链接文字含格式（`[**加粗**](ref:...)`）| 文字内格式保留 |

## 批 P-E：往返与回归

| # | 场景 | 关注点 |
|---|---|---|
| PE1 | 粘贴 → 保存 → 阅读态渲染**等价**（各元素还原）| 往返对称 |
| PE2 | **编辑态所见 = 阅读态所得**（对称性——B 核心承诺）| 不割裂 |
| PE3 | 纯文本粘贴（非 markdown）→ 原样插入 | 回归保护（不误伤普通粘贴）|
| PE4 | 粘贴后仍可正常编辑/保存 | 编辑器不破坏 |

## 批 P-F：粘贴位置与上下文（实现难点）

| # | 场景 | 关注点 |
|---|---|---|
| PF1 | 空行/空段落粘贴 | 基础块级插入 |
| PF2 | **段落文字中间**粘贴（粘贴 `# 标题` 到段落中）| 块级插行内 → 拆段/破坏段落？|
| PF3 | 行首/行尾粘贴 | 边界衔接 |
| PF4 | 列表内部粘贴 | 与现有列表融合/拆分 |
| PF5 | **代码块内部粘贴** | 应作为代码文本（不解析）还是退出代码块？|
| PF6 | 引用块内部粘贴 | 融合行为 |
| PF7 | 上下文合并（粘贴 H1 紧挨段落）| 多余空行/结构错乱 |

## 批 P-G：title/summary 粘贴（纯文本隔离）

| # | 场景 | 关注点 |
|---|---|---|
| PG1 | 粘贴 markdown 到 **title** → 保持纯文本原样 | title 是 `<input type="text">`，不受 markdown 影响（回归保护）|
| PG2 | 粘贴 markdown 到 **summary** → 保持纯文本原样 | summary 同为原生 input |
| PG3 | 边界：title 粘贴含 `#`/`/` 等特殊字符 → 保存 rename 文件名校验 | 文件名特殊字符拦截 |

---

## 执行计划

```
📋 阶段 1：场景矩阵落文档（本文件）——已完成 2026-08-13
📋 阶段 2：test_paste_markdown.py（当前红色，xfail 标记）——2026-08-14
📋 阶段 3：实现 B（handlePaste + ref-link 往返适配）
📋 阶段 4：P 系列测试转正
📋 阶段 5：全量回归（edit_switch 42 + hover 5 + paste N）
```

## 待确认项

- [ ] PF2/PF4/PF5（行中间/列表内/代码块内粘贴）：块级 HTML 插入行内的 ProseMirror schema 处理——可能需拆块/转纯文本兜底，实现时定
- [ ] PD3 空 ref 粘贴：B 实现后保持"不创建空链接"（与 renderer 空 ref 拦截一致）
- [ ] PG3 文件名校验：title 含特殊字符 → rename 行为的现有校验是否拦截
