# 编辑态切换 · 成体系测试 Plan（待开工）

> 状态：**PLAN（未开工）** — 2026-08-07 定稿，待用户确认后执行。
> 背景：编辑态 ↔ 导航切换区域连续出现多个 bug（内容残留、误 rename、误删、高亮错乱），
> 需要成体系的真实浏览器测试矩阵 + 自动化回归。

## 0. 测试环境（每轮前置）

```
1. 后端本地 8080 运行中（本地服务，无网络问题维度）
2. 浏览器强刷（清状态：localStorage 展开/收起、Alpine store）
3. 记录测试文档快照：文件名 / title / summary / content（编辑前后对比用，通过 API GET）
4. 测试文档：**新建固定成对测试文档**（避免污染现有数据，可跨项目）：
   - test-edit-A（根 common-knowledge）
   - test-edit-B（MyKnowledge 项目 common-knowledge）—— 与 A 成对（跨项目）
   - test-edit-C（Training 项目 common-knowledge）—— 与 A 成对
   - 每对含：多级标题（H1-H3，测目录）、互相 ref 引用（测引用联动）、正文段落
   - 测试后清理（删除进垃圾箱 + 清空，或 git 恢复）
5. 差异校验：**用 API 对比**（前端渲染内容 vs 后端 GET /api/document 返回），不做 git diff 硬校验
```

## 1. 三个维度

### ① 编辑入口 E（进入编辑的方式）
| 代号 | 入口 |
|---|---|
| E1 | 点击文档正文（view 态点击编辑器内部） |
| E2 | 点击标题输入框 |
| E3 | 点击摘要输入框 |

### ② 编辑态修改状态 M
| 代号 | 状态 |
|---|---|
| M0 | 无修改（进入立即切走） |
| M1 | 只改正文 |
| M2 | 只改标题（合法 rename 场景） |
| M3 | 只改摘要 |
| M4 | 改正文 + 标题（组合） |

### ③ 切换目标 T（从编辑态导航到）
| 代号 | 目标 | 方式 |
|---|---|---|
| T1 | 同项目另一文档 | 侧栏项目树文档行 |
| T2 | 跨项目文档 | 侧栏项目树文档行 |
| T3 | 顶层项目页 | 侧栏项目行 |
| T4 | 子项目页 | 侧栏子项目行 |
| T5 | 仪表盘 | 侧栏入口 |
| T6 | 垃圾箱 | 侧栏入口 |
| T7 | 引用链接文档 | 正文 ref 链接点击 |
| T8 | 返回上一文档 | back 按钮 / 面包屑 |
| T9 | 直接改 URL hash | 地址栏/外部链接 |

## 2. 检查点（每场景 6+3 项）

```
① 原文档：编辑模式已关闭？修改内容已保存？（对照 M 状态）
② 原文档：git diff 只有预期修改？**文件名未被篡改**（rename bug）？
③ 侧栏高亮：当前项 text+bg、直接父级 text-only、更上层恢复正常
④ 当前页：前端渲染内容 = 后端 GET 返回内容（防"只前端变后端没变"）
⑤ 当前页：标题/正文/目录/引用面板渲染符合预期
⑥ 回原文档：渲染正确、已保存的修改保留
⑦ rename 检查：文件名 = 预期（M2 时为新标题，M0/M1/M3 时不变）
⑧ summary 检查：原文档摘要不被新文档污染
⑨ toast 检查：符合预期（已保存/已改名/错误，无异常弹窗）
```

## 2.1 重复加载检测（每个场景必查，2026-08-07 新增）

> 背景：本项目"某资源被多次加载"是高频 bug 类型——一次导航/保存应只触发
> **1 轮**文档加载（doc+refs+meta 各 1 次），实测历史上有 2-4 轮（router 双触发、
> store init() 双执行导致双 hashchange 监听/双 SSE 订阅、本端保存触发 SSE 重载、
> 组件 init 兜底并发）。每个场景都要断言加载次数。

**工具**：`tests/frontend/api_tracker.py` 的 `ApiTracker`（Playwright 共用）

```python
from api_tracker import ApiTracker
tracker = ApiTracker(page)                    # 绑定后自动记录 /api/ 请求
tracker.reset()                               # 动作前清零
...执行动作（编辑→切换）...
tracker.assert_document_loads(doc_path, max_loads=1, label="切到 C")  # 主加载 ≤1
tracker.assert_method_count("PUT", 1, path_contains="document")       # 保存 ≤1
# 一轮加载 = GET doc + GET doc/refs + GET doc/meta（各 1，可分别断言）
```

**规则**：
- 一次导航：目标文档主加载 ≤1 次（`doc`/`refs`/`meta` 各 ≤1）
- 一次保存：`PUT /api/document/*` ≤1 次（防双保存）
- 本端保存后 3s 内不得出现 SSE 触发的重载波（后端 2s 轮询 version）
- 阈值放宽的场景必须注明原因（如远端多客户端同步测试）

## 3. 场景矩阵（交叉生成，按优先级分三批）

### 批 1：核心场景（必须全过）
| # | 场景 | 关键检查 |
|---|---|---|
| S1 | E1+M0→T1 | 基础：A 零变化保存、B 渲染正确、高亮 B+父级 |
| S2 | E1+M1→T1 | A 正文已保存；B 无 A 残留（防内容残留） |
| S3 | E1+M2→T1 | A **合法 rename** 为新标题 + 引用联动；B 正常 |
| S4 | E1+M3→T1 | A 摘要已保存、不被 B 污染 |
| S5 | E1+M0→T3 | A 保存关编辑；项目页正常；高亮=项目行 active |
| S6 | E1+M1→T5 | A 保存；仪表盘正常；无高亮残留 |
| S7 | E1+M1→T8 | back 回 A：修改保留、编辑已关 |
| S8 | E1+M0→T2 | 跨项目：两项目树正常、B 高亮+父级 |
| S9 | E1+M1→T1→T1 | **快速连续切换**（A→B→C）：无残留、竞态安全 |
| S10 | E1+M0→T7 | 引用链接：A 保存、B 加载、引用面板正常 |

#### 批 1 补充：原地保存场景（2026-08-09 手测发现，提交 1920998）

> 原 S4 只覆盖"切走"（T1）——切走再切回会重新 GET，天然掩盖显示问题。
> 手测发现**原地保存**（点非编辑区域 / 摘要框 Enter，不切走）是摘要回滚的必现场景。

| # | 场景 | 关键检查 |
|---|---|---|
| S4b | 编辑改摘要 → **原地保存**（不切走） | 摘要显示 = 保存值 = 后端 GET（防回滚） |
| S4c | 连续多次原地保存（≥3 次） | 每次显示 = 后端（防累积回滚） |
| S4d | 改正文 → 原地保存 | 正文不回滚（htmlContent 快照机制） |
| S4e | 改标题 → 原地保存（rename） | 标题 = 新名、hash 更新、**刷新不 404** |

**三类字段更新路径（决定回滚风险，2026-08-09 结论）**：
| 修改对象 | 显示数据来源 | 原地保存回滚风险 |
|---|---|---|
| 正文 | `htmlContent`（exitEdit 保存后快照编辑器 DOM） | 无 |
| 标题 | `document.title`（`_maybeRename` 自行更新 + replaceState hash） | 无 |
| 摘要 | `document.summary`（依赖 saveDocument 合并后端返回） | **有**——后端 PUT 返回缺 summary 字段；已用 body.summary 补齐修复 |

**经验**：切走（重新 GET）是"遮羞布"，原地保存才是真实校验点；每个字段都要测"原地保存后显示=后端"。

### 批 2：边界场景
| # | 场景 | 关键检查 |
|---|---|---|
| S11 | E2+M0→T1 | 标题输入框聚焦时切换（输入框值=污染源，必须不 rename） |
| S12 | E3+M0→T1 | 摘要输入框聚焦时切换（summary 不被污染） |
| S13 | E1+M0→T4 | 子项目页：正常 + 高亮（子项目行 active、顶层 text-only） |
| S14 | E1+M0→T6 | 垃圾箱：正常进入、A 已保存关编辑 |
| S15 | E1+M2→T8 | 改标题后 back：rename 生效且路由/视图一致（历史 bug） |
| S16 | E1+M4→T1 | 改正文+标题：都保存 + rename |
| S17 | E1+M1→T9 | 直接改 URL：A 保存、B 从外部链接加载正常 |
| S18 | E1+M0→A(再进) | 同文档重复进出编辑（edit→view→edit）不异常 |
| S19 | 编辑态点目录 TOC | 同文档内滚动跳转：**不退出编辑**、不高亮错乱 |
| S20 | 编辑态点项目树 chevron | 展开/收起：编辑状态保持、不高亮错乱 |

### 批 3：异常/竞态场景
| # | 场景 | 关键检查 |
|---|---|---|
| S21 | isLocked 时切换 | **应被阻止**（不保存不切，锁提示） |
| S22 | A→B→A 快速往返 | A 第二次显示已保存内容；无 rename/污染 |
| S23 | 编辑态点删除按钮 | 删除按钮**保留可见**；点击 → **自动退出编辑（保存）→ 弹删除确认框** → 确认删除进垃圾箱；检查：无保存冲突/无误 rename/删除后 404 可恢复 |
| S24 | 编辑态 + 项目树自动展开 | 进入深层文档自动展开与高亮不冲突 |
| S25 | 保存 409 冲突时切换 | 弹 diff / 不丢内容 / 切换不破坏 |
| S26 | 连续 5 次快速切换 | 无累积残留、最终文档正确、git diff 干净 |

## 4. 现有测试覆盖差距（已审查）

| 现有测试 | 覆盖 | 差距 |
|---|---|---|
| tests/frontend/test_smoke.py（Playwright） | 静态渲染/路由/主题/垃圾箱视图 | **无编辑态切换交互** |
| frontend/tests/save-regression.mjs（jsdom） | 保存零 diff、斜杠插入、setContent 切文档 | 单元级，不经过真实路由/侧栏/高亮/竞态 |
| rename-logic / diff-logic / trash-prompt-logic | 纯函数 | 不涉及交互 |

**结论**：需要新增**浏览器端到端测试**（Playwright，复用 test_smoke.py 的 static_server + 后端 8080 基础设施），
覆盖批 1（核心 10 场景）+ 批 2 关键场景。差异校验用 **API 对比**（渲染 vs 后端 GET），不做 git diff 硬校验。

## 5. 执行计划（明天开工）

```
阶段 1：新建固定测试文档（test-edit-A/B/C，成对跨项目）
阶段 2：手测批 1（S1-S10）→ 记录结果，修发现的新 bug
阶段 3：手测批 2/批 3（S11-S26）→ 修 bug
阶段 4：把批 1 固化为 Playwright 自动化（tests/frontend/test_edit_switch.py）
        断言：保存正确 / 无 rename / API 对比渲染=后端 / 高亮正确
阶段 5：纳入全量回归（pytest tests/ -q）
```

## 6. 已确认项

- [x] 测试文档：**新建固定成对**（test-edit-A/B/C，跨项目），测试后清理
- [x] S23 编辑态删除：删除按钮**保留可见**，点击 → **自动退出编辑（保存）→ 弹删除确认**（删除按钮在编辑区外，点击应触发"点外部退出编辑"；若时序异常则是 bug）
- [x] 差异校验：**API 对比**（渲染 vs 后端 GET），不做 git diff 硬校验
