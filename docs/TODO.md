# 前端待办清单（TODO）

> 更新：2026-08-04。按优先级排序，完成即划掉。

## 第一波（进行中）：飞书浮动工具栏核心

- [x] tooltip 延迟缩短（原生 title → 自定义 data-tip，0.18s 延迟）
- [x] 下划线（underline 扩展 + `<u>` 内联 HTML 存储 + turndown 规则）
- [x] T 下拉（块类型：正文 / 标题 1-4 / 有序 / 无序列表）
- [x] 缩进按钮（列表嵌套升/降级）
- [x] 链接自定义浮层（替代浏览器 prompt，选中文字作显示名）

## 第二波：富文本扩展（需先定"markdown 内联 HTML 存储"方案）

- [ ] 对齐：左 / 中 / 右（markdown 不支持 → `<p style="text-align:...">` 内联 HTML）
- [ ] 字体颜色（A 图标 → 色板，`<span style="color:...">`）
- [ ] 背景色 / 高亮（`<span style="background:...">`，含"恢复默认"）
- [ ] 技术方案确认：marked raw HTML 保留 / turndown span 规则 / 回归新增用例

## 已知小 bug

- [ ] 锁状态过期不刷新 UI（checkLock 只在 init 跑，后端锁过期前端仍显示"AI 正在整理"）
- [ ] 进页面即出现的 "Node cannot be found" warning（待定位）

## 体验补全

- [ ] + 加号菜单（行首 hover，飞书同款）
- [ ] 区块 ⋮⋮ 菜单（可选，非核心）
- [ ] 文档卡片 hover 重命名/删除按钮（dashboard 卡片，C2 设计已定：hover 右上角浮出，删除二次确认弹窗）
- [ ] 动态 icon 接入（`cardIconSvg()` 已写好未调用，关键词→SVG 映射）
- [ ] 待讨论：E2 元信息展示时机（正文 vs 折叠，设计原文 FRONTEND_INTERACTION_FEEDBACK.md）

## 离线场景（A2 部分实现）

- [ ] 全屏阻断弹窗：导航到新页面 / 冷启动离线无草稿 / Setup 页（当前只有横幅 + 草稿恢复）
- [ ] toast 提示（3s）：点击操作按钮（新建/删除/保存）时离线
- [ ] 冷启动离线 + 有草稿 → 直接进编辑恢复模式（5b 场景）

## 数据一致性（后端协作时讨论）

- [ ] 文档内容被"旧编辑会话"保存覆盖（改文档内容时若有该文档的编辑会话开着，退出保存会覆盖新内容）——需锁/版本冲突机制
- [ ] 锁机制 / git commit 策略 / AI 整理时机（后端 AI 协作讨论）
