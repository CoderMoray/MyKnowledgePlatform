# ── Runtime dependencies (same as pyproject.toml) ──────────

# MCP protocol
mcp>=1.0.0

# YAML frontmatter
pyyaml>=6.0

# Git operations
gitpython>=3.1

# REST API
fastapi>=0.100.0,<0.111.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Pydantic models
pydantic>=2.0

# SSE event versioning
aiosqlite>=0.19

# ── Development extras ─────────────────────────────────────

# OSS cloud sync (optional, enabled via config)
oss2>=2.18.0

# Automation scheduler (optional SyncJob)
apscheduler>=3.10.0
